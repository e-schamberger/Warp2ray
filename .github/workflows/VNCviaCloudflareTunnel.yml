name: VNC via Cloudflare Tunnel
on: workflow_dispatch

jobs:
  deploy-vnc:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
    # مرحله 1: نصب VNC
    - name: Setup VNC Server
      run: |
        sudo apt update
        sudo apt install -y xfce4 tightvncserver
        mkdir -p ~/.vnc
        echo -e "1aq2sw@3de\n1aq2sw@3de\nn" | vncpasswd
        vncserver :1 -geometry 1280x800 -localhost

    # مرحله 2: احراز هویت Cloudflare
    - name: Authenticate Cloudflared
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      run: |
        wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
        chmod +x cloudflared
        ./cloudflared tunnel login
        mkdir -p ~/.cloudflared
        cp ~/.cloudflared/cert.pem ./

    # مرحله 3: راه‌اندازی تونل
    - name: Start Cloudflare Tunnel
      env:
        CLOUDFLARE_TUNNEL_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
      run: |
        cat > config.yml <<EOL
        tunnel: github-vnc-tunnel
        credentials-file: ./tunnel.json
        originCert: ./cert.pem
        ingress:
          - hostname: vnc.net2ray.dpdns.org
            service: tcp://localhost:5901
            originRequest:
              connectTimeout: 30s
          - service: http_status:404
        EOL
        
        nohup ./cloudflared tunnel --config config.yml run > tunnel.log 2>&1 &
        sleep 20
        cat tunnel.log

    # مرحله 4: نگهداشت اتصال
    - name: Keep Alive
      run: |
        echo "✅ VNC Ready at vnc.net2ray.dpdns.org:5901"
        echo "🔑 Password: 1aq2sw@3de"
        while true; do
          sleep 60
          echo "[$(date)] Connection active"
        done
